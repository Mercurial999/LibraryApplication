export default class Borrower {
    constructor(fullName, email, role, gradeLevel, password) {
      this.fullName = fullName;
      this.email = email;
      this.role = role;
      this.gradeLevel = gradeLevel;
      this.password = password;
    }
  }
  